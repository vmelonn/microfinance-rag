# service
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
psycopg[binary]==3.2.3

# ingest (offline only, never installed in the cluster image)
# see PLAN.md limit 2: the namespace cannot host an embedding model
sentence-transformers==3.3.1 ; extra == "ingest"
beautifulsoup4==4.12.3
lxml==5.3.0

# eval
pyyaml==6.0.2
pytest==8.3.4
